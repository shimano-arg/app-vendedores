rules_version = '2';

// PLACEHOLDER — reglas reales bajadas del proyecto en E1.
// Este archivo se REESCRIBE en la etapa E1 (Rules) con la baseline
// de producción + refactor cerrado + suite de tests contra emulator.
// NO deployar este placeholder. NO tocar hasta E1.

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
